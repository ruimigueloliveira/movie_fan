apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rental-pvc
  namespace: egs9
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi
