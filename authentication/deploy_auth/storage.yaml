apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-pvc
  namespace: egs9
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi
